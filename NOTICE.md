# Notices

This repository combines new gateway code with modified/reference code from related PPPP camera projects.

- `PPPP/` is based on `datenstau/A9_PPPP`, declared as ISC in its `package.json`.
- `pppp-dissector/` is MIT licensed by Magnus Ihse Bursie; see `pppp-dissector/LICENSE`.
- `aiopppp/` is Apache-2.0 licensed; see `aiopppp/LICENSE`.
- `a9serv/` is a small C proxy attributed in its README to `hyc` and based on A9_PPPP concepts.

The local project code in `bkcam-server/` is provided as-is for use with cameras you own on networks you control.
