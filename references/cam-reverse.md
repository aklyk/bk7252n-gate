# cam-reverse

The local handoff workspace contained a `cam-reverse/` checkout that was useful during exploration, but it did not include a clear license file. Its source is not copied into this public repository.

Keep it as a local/private research reference unless the upstream licensing situation is clarified.
